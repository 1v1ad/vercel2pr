GGRoom BACKEND PATCH — 20251111-1737
Files:
- server.js — включён в архив для полноты (без правок).
- src/routes_admin.js — добавлены эндпоинты:
  * GET   /api/admin/cluster?hum_id=H
  * POST  /api/admin/unmerge { hum_id, user_ids:[..], reason }
  * GET   /api/admin/merge_suggestions?limit=N
  * POST  /api/admin/merge_apply { primary_id, secondary_id }

Требуемые ENV:
- ADMIN_PASSWORD=<ваш пароль> (или ADMIN_PWD)
- DATABASE_URL=postgres://... (Neon)
- JWT_SECRET=... (для auth, если используется)
- DEVICE_ID_HEADER (опционально; по умолчанию x-device-id)

Проверка:
- GET  /api/admin/cluster?hum_id=1  (с заголовком X-Admin-Password)
- POST /api/admin/unmerge
- GET  /api/admin/merge_suggestions
- POST /api/admin/merge_apply
